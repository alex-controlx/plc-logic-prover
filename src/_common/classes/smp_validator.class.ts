import {ITagUI} from "../interfaces";


/**
 * This is Schneider Modbus Protocol Validator
 */

export default class SmpAddressHelper {

    static isValidTag(abTag: ITagUI): boolean {
        throw new Error("Method isn't implemented. Used here: " + [abTag].join(","));
    }

}
